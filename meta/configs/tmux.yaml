- defaults:
    link:
        create: true
        relink: true
    shell:
        stdin : true
        stdout: true
        quiet : false

- clean: ['~']

- link:
    ~/.tmux.conf : tmux/tmux.conf
    ~/.tmuxp     : tmux/tmuxp

- shell:
    - [./scripts/installation/tmux.sh, Installing TMUX]

