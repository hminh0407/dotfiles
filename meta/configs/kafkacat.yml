# A fast & lightweight configurable terminal
- defaults:
      shell:
          stdin: true
          stdout: true
          quiet: false
      link:
          create: true
          relink: true
          force: true
- shell:
      - [
            "sudo apt-fast install --no-install-recommends -y kafkacat",
            Installing Kafkacat,
        ]
